<% recording_studio_page_nav(title: "Dummy host") %>

<div class="flex w-full flex-col gap-6">
  <%= render FlatPack::PageTitle::Component.new(
    title: "Dummy host",
    subtitle: "This app proves the sitemaps gem in a real host. It is not the product.",
    variant: :h1
  ) %>

  <%= render FlatPack::Card::Component.new(style: :default) do |card| %>
    <% card.body do %>
      <p class="text-sm leading-6 text-(--surface-content-color)">
        Sign in stays at admin@admin.com. Sitemap XML is not on this screen yet.
      </p>
    <% end %>
  <% end %>
</div>
