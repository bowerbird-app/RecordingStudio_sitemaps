# Recording Studio Sitemaps 0.1.0

This repo is now Recording Studio Sitemaps, not the addon starting point.

- Gemspec: `recording_studio`, `~> 4.2` and `recording_studio_accessible`, `~> 0.6`
- Dummy GitHub tags: Recording Studio `v4.2.0`, Accessible `v0.6.1`, Root Switchable `v0.5.0`, FlatPack `v0.1.133`
- Authenticated dummy layout: `RecordingStudio::UsesDefaultLayout` plus Flatpack CSS/JS (including Devise sign-in)
- Public README is the product. `docs/gem_template/` stays as engine internals
- This slice does not ship `/sitemap.xml`, generation logs, Admin, or Publishable wiring
